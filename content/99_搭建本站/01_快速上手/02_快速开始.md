---
title: 快速开始
description: 从克隆仓库到本地启动，5 分钟跑通整个项目。
author: Refinex
createdAt: 2026-02-22
updatedAt: 2026-02-22
---

# 快速开始

## 环境准备

开始之前，请确保本地已安装：

- **Node.js** ≥ 18（推荐 LTS 版本）
- **npm** ≥ 9（随 Node.js 一起安装）

验证安装：

```bash
node -v   # 应输出 v18.x 或更高
npm -v    # 应输出 9.x 或更高
```

## 克隆与启动

```bash
# 1. 克隆项目
git clone https://github.com/refinex-lab/Refinex-Blog.git
cd Refinex-Blog

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev
```

启动成功后，终端会输出本地访问地址（通常是 `http://localhost:5173`），在浏览器中打开即可。

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动 Vite 开发服务器，支持热更新 |
| `npm run build` | 先执行 TypeScript 类型检查，再构建生产产物到 `dist/` |
| `npm run preview` | 本地预览生产构建产物 |
| `npm run lint` | 运行 ESLint 代码检查 |

## 写第一篇文档

在 `content/` 目录下新建一个 Markdown 文件，项目会自动识别并渲染。

例如，创建 `content/01_开始使用/02_我的第一篇.md`：

```markdown
---
title: 我的第一篇文档
description: 这是一篇测试文档。
---

# Hello World

这是我的第一篇文档，支持 **GFM** 语法。
```

保存后刷新页面，左侧目录树中就会出现这篇文档。

## 下一步

了解内容目录的组织规则和命名约定。
